import { createConnection } from 'typeorm';

// essa funcao ela vai preocurar no meu projeto o arquivo ormconfig, para ele fazer a conexao, se ele encontrar ele ja vai fazer a conexao
createConnection();
